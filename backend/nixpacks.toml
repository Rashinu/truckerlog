[phases.setup]
nixPkgs = ["python312", "python312Packages.pip"]

[phases.install]
cmds = ["python3 -m pip install -r requirements.txt"]

[phases.build]
cmds = ["python3 manage.py collectstatic --noinput", "python3 manage.py migrate"]

[start]
cmd = "gunicorn backend.wsgi --bind 0.0.0.0:$PORT"
